#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int main() {
    string n;
    cin >> n;

    while(i>(int)n)

    int A = count(n.begin(), n.end(), 'A');
    int B = count(n.begin(), n.end(), 'B');

    if(A>B){
        cout << "A";
    }else {
        cout << "B";
    }

    return 0;
}
